#include <bits/stdc++.h>
using namespace std;

const int MAXN = 100009;

int arr[MAXN];

int main() {
	cin.tie(0); ios_base::sync_with_stdio(0);
	int N = 200000;
	cout << N << '\n';
	for (int i = 0; i < N; ++i) cout << -100 << ' ' << 2 << '\n';
	return 0;
}