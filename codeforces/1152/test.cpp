#include <bits/stdc++.h>
using namespace std;

typedef long long lli;

const int MAXN = 100009;

int main() {
    cin.tie(0); ios_base::sync_with_stdio(0);

    lli a, b;
    cin >> a >> b;
    cout << (a + b) << '\n';

    return 0;
}